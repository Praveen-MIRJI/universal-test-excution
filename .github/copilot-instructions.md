# Universal Test Execution

- Keep runtime code under `src/` and preserve the module boundaries represented by its subdirectories.
- Use the official MCP TypeScript SDK: https://github.com/modelcontextprotocol/typescript-sdk
- Keep generated reports, screenshots, and local data out of source control.
- Run `npm run typecheck`, `npm run lint`, and `npm test` before submitting changes.
